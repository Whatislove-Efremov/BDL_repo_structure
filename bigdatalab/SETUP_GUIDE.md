# 📚 Полное руководство по настройке BigDataLab

> **Для кого это руководство?**  
> Это руководство написано для людей, которые только начинают знакомиться с программированием. Мы объясним каждый шаг максимально подробно.

Полное руководство доступно по адресу: https://github.com/your-org/bigdatalab/blob/main/SETUP_GUIDE.md

## 🚀 Быстрый старт

### 1. Установите необходимые инструменты:
- Python 3.12+
- uv (менеджер пакетов)
- Git
- Docker Desktop

### 2. Установите зависимости:
```bash
uv sync
```

### 3. Активируйте виртуальное окружение:
```bash
source .venv/bin/activate  # macOS/Linux
.venv\Scripts\activate     # Windows
```

### 4. Настройте переменные окружения:
```bash
cp env.example .env
# Отредактируйте .env и измените пароли
```

### 5. Запустите Docker сервисы:
```bash
docker-compose up -d
```

### 6. Запустите тесты:
```bash
uv run pytest
```

## 📚 Основные команды

### Проверка кода (линтер):
```bash
uv run ruff check .           # Проверить код
uv run ruff check --fix .     # Исправить автоматически
uv run ruff format .          # Отформатировать код
```

### Запуск тестов:
```bash
uv run pytest                 # Все тесты
uv run pytest -v              # С подробным выводом
uv run pytest tests/test_main.py  # Конкретный файл
```

### Docker команды:
```bash
docker-compose up -d          # Запустить сервисы
docker-compose ps             # Статус сервисов
docker-compose logs           # Просмотр логов
docker-compose stop           # Остановить сервисы
docker-compose down           # Остановить и удалить
```

### Git команды:
```bash
git status                    # Статус изменений
git add .                     # Добавить все файлы
git commit -m "message"       # Создать коммит
git push                      # Отправить на сервер
```

## 🌐 Доступ к сервисам

После запуска `docker-compose up -d`:

- **PostgreSQL**: `localhost:5432`
  - База: bigdatalab
  - Пользователь/Пароль: из .env
  
- **pgAdmin**: http://localhost:5050
  - Email/Пароль: из .env
  
- **JupyterLab**: http://localhost:8888
  - Без пароля (для разработки)

## 📖 Полная документация

Для подробных инструкций смотрите файл SETUP_GUIDE.md в корне проекта.

## ❓ Частые проблемы

### "uv: command not found"
```bash
# Установите uv:
curl -LsSf https://astral.sh/uv/install.sh | sh
# Перезапустите терминал
```

### "Docker daemon not running"
```bash
# Запустите Docker Desktop приложение
```

### "Port already in use"
```bash
# Измените порт в docker-compose.yml или остановите процесс:
docker-compose down
```

### ModuleNotFoundError
```bash
# Убедитесь что виртуальное окружение активировано:
source .venv/bin/activate
# Запускайте через uv:
uv run pytest
```

## 💡 Чеклист ежедневной работы

**Начало дня:**
1. `source .venv/bin/activate` - активировать окружение
2. `docker-compose up -d` - запустить сервисы
3. `git pull` - получить изменения

**Во время работы:**
1. Пишите код в `homework/`
2. Пишите тесты в `tests/`
3. `uv run ruff check .` - проверяйте код
4. `uv run pytest` - запускайте тесты

**Перед коммитом:**
1. `uv run ruff format .` - отформатировать
2. `uv run ruff check .` - проверить
3. `uv run pytest` - тесты
4. `git add .` - добавить файлы
5. `git commit -m "..."` - коммит

**Конец дня:**
1. `git push` - отправить изменения
2. `docker-compose stop` - остановить сервисы

---

**Нужна помощь? Смотрите полное руководство в SETUP_GUIDE.md!**
